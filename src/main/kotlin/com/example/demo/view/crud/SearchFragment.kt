package com.example.demo.view.crud

import com.example.demo.model.Account

class SearchFragment {

    /*private var baseList: List<Account> = listOf()
    private var filteredList: MutableList<Account> = mutableListOf()

    fun searchList() {
        filteredList.clear()
        baseList.forEach {
            if (it.contains(search.text.toString())) {
                filteredList.add(it)
            }
        }
        adapter.updateList(filteredList)
    }
    fun updateList(filteredList: List<Account>) {
        list.clear()
        list.addAll(filteredList)
        notifyDataSetChanged()
    }*/


}